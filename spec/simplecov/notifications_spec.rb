require 'spec_helper'

describe SimpleCov::Notifications do
  it 'has a version number' do
    expect(SimpleCov::Notifications::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
